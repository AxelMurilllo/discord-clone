import React from "react";
import { IChannelBtn } from "../../shared/types";

const ChannelBtns = React.memo(({icon, img, title, url}: IChannelBtn) => {
    return (
        <div className="channel-btn">

        </div>
    )
})

export default ChannelBtns;